class Solution {
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        
        int low = Math.min(nums1[0], nums2[0]);
        int high = Math.max(nums1[nums1.length - 1], nums2[nums2.length - 1]);

        

    }
}
