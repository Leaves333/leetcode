#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    long long countSubarrays(vector<int>& nums, int minK, int maxK) {
        // for max, idx of first element on l/r > max
        // for min, idx of first element on l/r < min
    }
};
