#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    long long maximumScore(vector<vector<int>>& grid) {
        long long dp[grid.size()][grid.size()][grid.size()];
        memset(dp, 0, sizeof(dp));

    }
};
